# /etc/nginx/sites-available/faktura.conf
# certbot --nginx -d api.faktura.ru  добавит TLS-блок автоматически.

server {
    listen 80;
    server_name api.faktura.ru;

    # Лимиты, чтобы можно было заливать .glb / .usdz / фото
    client_max_body_size 100M;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;

    location / {
        proxy_pass http://127.0.0.1:8000;  # kong из docker-compose
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
    }
}
